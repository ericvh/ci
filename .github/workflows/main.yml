name: Linux Kernel Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    container:
      image: ghcr.io/v9fs/docker:latest
      options: -u root
    steps:
    - name: test
      run: echo hello squidboy
